<!--Sneaky HTML comment to persuade my text editor to highlight as HTML-->
{{define "content"}}
<head><title>{{.User.Username}}</title></head>
<script type="text/javascript">
{{if .SameUser}}
	function toggleAvatarForm() {
		let p = document.getElementById("pfp-form-container");
		if (p.style.display === "none") {
			p.style.display = "block";
		} else {
			p.style.display = "none";
		}
	}

	let pfp = document.getElementById("pfp");
	let pfpformc = document.getElementById("pfp-form-container");
{{end}}

function toggleModal(m) {
	slides(m, 0);
	if (document.getElementById("lbox-"+m).style.display === "none") {
		document.getElementById("lbox-"+m).style.display = "block";
	} else {
		document.getElementById("lbox-"+m).style.display = "none";
	}
}

function slides(gallery, slide) {
	let i;
	let x = document.getElementById(`lbox-${gallery}`).getElementsByClassName(`slide`)
	if (slide < 0) {
		slide = x.length - 1;
	} else if (slide > x.length - 1) {
		slide = 0;
	}
	for (let i = 0; i < x.length; i++) {
		x[i].style.display = "none";
	}
	x[slide].style.display = "inline";
}

window.onclick = function(event) {
	let cname = event.target.className;
	let id = event.target.id;
	if (cname == "lbox" && id != "pfp-form-container") {
		let n = id.match(/lbox-(\d+)/)[1];
		toggleModal(n);
	}
	{{if .SameUser}}
	if (id == "pfp") {
		toggleAvatarForm();
	}
	if (id == "pfp-form-container") {
		toggleAvatarForm();
	}
	{{end}}
}

function changeSlide(gallery, direction) {
	let x = document.getElementById(`lbox-${gallery}`).getElementsByClassName(`slide`);
	for (let i = 0; i < x.length; i++) {
		if (x[i].style.display != "none") {
			slides(gallery, i + direction);
			return;
		}
	}
}
</script>

<div id="body">

	{{if .SameUser}}
	<div id="pfp-form-container" class="lbox" style="display: none;"> <!--I don't know if reusing lbox here is a good idea but I'm doing it anyway-->
		<form id="pfp-form" action="/update_avatar" method="post" style="width: 600px; height: 200px; background-color: white;display: inline-block;">
			<label for="new-pfp-url">URL</label><input type="text" name="url" id="new-pfp-url">
			<input type="submit">
		</form>
	</div>
	{{end}}

	<div id="user-info">
		<img width="250" height="250" src="{{.User.Avatar}}" id="pfp">
		{{if .SameUser}}
		<p style="display: inline;">Welcome to your profile, {{.myUser.Displayname}}</p><br>
		{{end}}
		<strong>{{.User.Displayname}}</strong><br>
		@{{.User.Username}}
	</div>
	
	<div id="img-grid">
		{{range $gi, $g := .Galleries}}
			{{if $g.Photos}}
				<div class="gallery-thumb">
					<img src="{{$g.Thumb}}" alt="{{$g.Id}}" onclick="toggleModal({{$gi}})">
				</div>

				<div class="lbox" id="lbox-{{$gi}}" style="display: none;">
					<div class="prev" onclick="changeSlide({{$gi}}, -1)"><<</div><div class="next" onclick="changeSlide({{$gi}}, 1)">>></div>
					{{range $pi, $p := $g.Photos}}
						<span class="slide" id="slide-{{$gi}}:{{$pi}}">
							<table class="infobox">
								{{range $k, $v := $p.Exif}}
								<tr>
									<td>{{$k}}</td>
									<td>{{$v}}<td>
								</tr>
								{{end}}
							</table>
							<img src="{{ $p.Reference }}" class="photo"> <!---->
						</span>
					{{end}}
					
					<div class="infobox gal-info">
						<span class="description">
						{{$g.Description}}
						</span>
						<span class="gallery-upload-stamp">
						{{$g.Uploaded}}
						</span>
					</div>
				</div>
			{{end}}
		{{end}}
	</div>
</div>
<style>
#body {
	margin-left: auto;
	margin-right: auto;
	max-width: 90rem;
}

.gallery-thumb {
	padding: 1px;
	width: 30%;
	height: 30%;
	display: block;
	margin-left: auto;
	margin-right: auto;
}
.gallery-thumb img {
	max-width: 100%;
	padding-top: 25px;
	object-fit: cover;
	max-height: 100%;
}

.lbox {
	position: fixed;
	z-index: 1;
	width: 100%;
	height: 100%;
	left: 0;
	top: 0;
	text-align: center;
	background-color: rgb(0, 0, 0, 0.8);
}

.lbox img {
  display: block;
  margin-left: auto;
  margin-right: auto;
}

.prev, .next {
	color: white;
	display: inline;
	background-color: black;
	margin-top: 25%;
}

#img-grid {
	max-width: 90rem;
	display: table;
	height: auto;
	outline: auto;
	display: flex;
	flex-wrap: wrap;
	margin-bottom: 3rem;
	margin-left: auto;
	margin-right: auto;
}

#user-info {
	margin-left: auto;
	margin-right: auto;
	margin-bottom: 3rem;
	width: 60rem;
}

.infobox {
	background-color: gray;
	display: inline-block;
	width: 500px;
	margin: 0px;
}
</style>

{{end}}
